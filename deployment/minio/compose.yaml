services:
  minio:
    image: quay.io/minio/minio
    command: server /data --console-address ":9001"
    ports:
      - 9000:9000
      - 9001:9001
    environment:
      MINIO_ROOT_USER: descenty
      MINIO_ROOT_PASSWORD: qweasdzxc
    volumes:
      - minio_data:/data

volumes:
  minio_data: